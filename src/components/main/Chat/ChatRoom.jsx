import { useState } from "react"
import styled, { css } from "styled-components"

const ChatRoom = ({ information }) => {
  return (
    <div>
      <h5>{information}</h5>
    </div>
  )
}

export default ChatRoom
